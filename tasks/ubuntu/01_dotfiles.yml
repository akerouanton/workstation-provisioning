---
- name: Create symlinks for dotfiles
  copy:
    src: "ubuntu/{{ item }}"
    dest: ~/
  with_items:
    - .agignore
    - .bash_aliases
    - .bashrc
    - .gitconfig
    - .gitignore
  tags:
    - dotfiles
